services:
  later:
    image: ghcr.io/zawnk/later-server:0.1.7@sha256:c55e1345b6cf00e29ab41d7876ec5626586d95a1283e070466a9eaea9e757c87
    # build: .   # uncomment (and comment out `image:` above) for a local/dev build instead of the published image
    container_name: later
    restart: unless-stopped
    environment:
      - TZ=Europe/Berlin
      - LATER_HTTP_LOG=0
    volumes:
      - ./data:/data
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD", "/later", "healthcheck"]
      interval: 30s
      timeout: 3s
      start_period: 5s
      retries: 3
